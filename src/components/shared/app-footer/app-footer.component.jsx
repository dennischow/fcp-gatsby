import * as React from "react";

const AppFooter = () => (
    <footer className="app-footer">
        <div>
            <small>Footer</small>
        </div>
    </footer>
);

export default AppFooter;
